# Core Django dependencies
Django>=5.2,<5.3
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
djangorestframework-simplejwt>=5.3.0
python-dotenv>=1.0.0
whitenoise>=6.6.0
# Pillow for ImageField - using minimal version
pillow>=10.0.0
gunicorn>=22.0.0
psycopg[binary]>=3.2.0
dj-database-url>=2.2.0
django-allauth>=65.4.0

# NOTE: Heavy dependencies are NOT included due to Vercel's 250MB Lambda limit.
# The app gracefully degrades when these are unavailable.
# To enable full features, deploy to a platform with larger limits (Render, Railway)
# and uncomment the following:
#
# AI Chatbot:
# google-genai>=0.6.0
#
# ML Predictions:
# scikit-learn>=1.3.0
# pandas>=2.0.0
# numpy>=1.24.0
# joblib>=1.3.0
