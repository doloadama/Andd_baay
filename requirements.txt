# Core Django dependencies
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
djangorestframework-simplejwt>=5.3.0
Pillow>=10.0.0
python-dotenv>=1.0.0
whitenoise>=6.6.0

# Optional: AI Chatbot (comment out if not needed)
google-generativeai>=0.3.0

# NOTE: ML dependencies (scikit-learn, pandas, numpy) are NOT included by default
# due to Vercel's 500MB Lambda size limit. The app uses fallback predictions.
# To enable ML features, deploy to a platform with larger limits (Render, Railway)
# and uncomment the following:
# scikit-learn>=1.3.0
# pandas>=2.0.0
# numpy>=1.24.0
# joblib>=1.3.0
