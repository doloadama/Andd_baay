# Environment
.env
env/
venv/
.venv/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# Django
db.sqlite3
staticfiles/
media/

# ML model (large binary, regenerated by training pipeline)
modele_rendement.pkl

# IDE
.idea/
.vscode/
.cursor/
*.swp

# OS
.DS_Store
Thumbs.db

# Local scratch / one-off scripts
identifier.sqlite
structure.txt
*_scratch.py
_smoke_*.py

.vercel

# Node (build tooling : Tailwind + Font Awesome subset). Voir perf-audit.md actions #2/#3.
# Les outputs (baay/static/css/tailwind.css, fa-subset.css, webfonts/*.woff2) SONT committés
# pour éviter d'imposer un build Node sur Vercel.
node_modules/
# package-lock.json est volontairement committé pour des installs reproductibles.
